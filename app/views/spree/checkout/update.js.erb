<%= render partial: 'spree/shared/flash_messages', locals: { sticky: true } %>

<% unless @order.valid? %>
  $('input[type="radio"]:checked').click()
  $('#order_state_lock_version').val(<%= @order.state_lock_version %>)
<% end %>
