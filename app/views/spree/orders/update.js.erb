document.getElementById('update-cart').outerHTML = "<%= j(render(partial: 'spree/orders/form')) %>"

<%= render partial: 'spree/shared/flash_messages', locals: { sticky: true, additional_class: 'slide-down' } %>

// fetch cart here instead of ajax:complete because original form was replaced
Spree.fetch_cart()
