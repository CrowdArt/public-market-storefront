= simple_form_for @user || Spree::User.new, as: :spree_user, url: spree.create_new_session_path, wrapper: :vertical_hint_label do |f|
  = f.input :username, label: Spree.t(:email_login), autofocus: true, input_html: { value: f.object.email.presence || session.delete(:email) }
  = f.input :password, label: Spree.t(:password)

  = f.button :submit, Spree.t(:sign_in), class: 'btn btn-primary btn-lg btn-block upcase'
