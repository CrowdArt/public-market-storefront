en:
  simple_form:
    "yes": 'Yes'
    "no": 'No'
    hints:
      defaults:
        website: Separate URLs with commas
        marketplaces: Separate URLs with commas
        software: Separate software/services with commas
        other_category: Separate each category with a comma
        sku_count: If you don’t know, submit your best estimate
    required:
      text: 'required'
      mark: '*'
      # You can uncomment the line below if you need to overwrite the whole required html.
      # When using html, text and mark won't be used.
      # html: '<abbr title="required">*</abbr>'
    error_notification:
      default_message: "Please review the problems below:"
